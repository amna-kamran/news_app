import 'package:flutter/material.dart';

class ArtNewsScreen extends StatelessWidget {
  const ArtNewsScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Padding(
      padding: EdgeInsets.all(50),
      child: Center(
        child: Text("Art"),
      ),
    );
  }
}
